pymysql==1.4.6
mysql-connector-python==8.0.28

# Notes:
# - mysql-connector-python 9.x caused a native crash on Windows + Python 3.13 in this environment.
# - We pinned 8.0.28 which successfully connected during diagnostics.pymysql==1.4.6Flask>=3.1.2
mysql-connector-python>=8.1.0

# Optional: pin Werkzeug if you need specific behavior
# Werkzeug>=3.1.3
